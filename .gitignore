# insightface buffalo_l weights for tools/face_drift.py -- 190 MB, fetched not vendored
tools/face_models/

# Python
__pycache__/
*.py[cod]
*.egg-info/

# Local-only working files.
# These two are personal setups: they load private/merged checkpoints and input
# images that are not published, so they would not run for anyone else.
# Kept on disk for the author, deliberately not committed.
workflows/h3_continuous_local_prompt.json
demo_8shots_workflow.json

# Editor / OS
.vscode/
.idea/
Thumbs.db
desktop.ini
.DS_Store
